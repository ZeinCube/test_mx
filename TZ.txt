Поднять git (проект в любом общедоступном хранилище кода)
Поднять docker, docker-compose
В отдельных контейнерах docker поднять nginx, php-fpm, MariaDB 10, PostgreSQL 12 используя docker-compose
Скачать ФИАС http://basicdata.ru/download/fias/ (или из любого другого источника) и развернуть его в PostgreSQL (можно только несколько регионов).
Написать простую страницу с поиском адресов ФИАС (PostgreSQL) по полям:
Регион
Город
Улица
Дом
Для найденных адресов добавить возможность сохранить их в текстовом виде в MariaDB (таблица с колонкой типа varchar1024)– отдельная кнопка
Для найденных адресов добавить возможность сохранить их в таблицу с полями (Регион, Город, Улица, Дом – таблица с колонками region, city, street, home типа varchar256) в MariaDB – отдельная кнопка
п.5,6,7 запросы к серверу делать аяксом, без библиотек. Рендер таблицы результатов делать на js
Исходники залить в git для п.6 и п.7 разными ветками одного проекта